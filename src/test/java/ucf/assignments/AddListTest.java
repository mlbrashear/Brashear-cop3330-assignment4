package ucf.assignments;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class AddListTest {

    @Test
    void add() {
        /*
        assertEquals(button bar is created)
        assertEquals(remove button is created)
        assertEquals(edit desc button is created)
        assertEquals(edit title button is created)
        assertEquals(edit date button is created)
        assertEquals(save button is created)
        assertEquals(show complete button is created)
        assertEquals(show incomplete button is created)
        assertEquals(scroll bar is created)
         */
    }
}